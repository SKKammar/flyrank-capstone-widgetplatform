PORT=3000
# Generate a secure minimum 32-character secret using:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=generate_strong_secret_min_32_chars_via_crypto_randomBytes
DB_FILENAME=./dev.sqlite
NODE_ENV=development
